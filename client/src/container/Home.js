import React from "react";
import {PageHeader} from 'react-bootstrap'
const Home = (props) => {
        return (
            <div>
                <PageHeader>Home</PageHeader>
            </div>
        );
};

export default Home;